[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"

# SPA-Fallback: alle Routen auf index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
